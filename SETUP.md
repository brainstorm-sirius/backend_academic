# Инструкция по настройке проекта

Этот файл содержит инструкции для новых разработчиков по настройке проекта с нуля.

## 📋 Предварительные требования

- Python 3.11 или выше
- pip (менеджер пакетов Python)
- Git

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd backend_academic
```

### 2. Создание виртуального окружения

```bash
python3 -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows
```

### 3. Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта (или используйте системные переменные):

```bash
# .env
SECRET_KEY=your-secret-key-here-change-in-production
DATABASE_URL=sqlite:///./users.db
ACCESS_TOKEN_EXPIRE_MINUTES=60
```

**⚠️ Важно**: Не коммитьте `.env` файл в git! Он уже добавлен в `.gitignore`.

### 5. Инициализация базы данных

База данных и все таблицы создаются автоматически при первом запуске приложения через `models.Base.metadata.create_all()`.

### 6. Импорт данных (опционально)

Для работы с незарегистрированными авторами нужно импортировать CSV файлы:

```bash
# Убедитесь, что CSV файлы находятся в корне проекта:
# - authors_expanded_with_ids.csv
# - authors_scientific_interests.csv

python import_csv.py
```

**Примечание**: CSV файлы не включены в репозиторий из-за их размера. Получите их у команды или создайте самостоятельно.

### 7. Настройка модели рекомендаций

Модель для рекомендаций должна находиться в папке `model/`:

```
model/
  ├── authors_data.pkl
  ├── vectorizer.pkl
  ├── author_vectors.npz
  └── knn_model.pkl
```

**Вариант 1**: Получить готовую модель у команды

**Вариант 2**: Обучить модель самостоятельно:

```bash
# Убедитесь, что файл authors_scientific_interests.csv находится в корне проекта
python train_model.py
```

**Примечание**: Папка `model/` не включена в git из-за размера файлов (~83MB).

### 8. Запуск приложения

```bash
uvicorn app.main:app --reload
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000`

- API документация: `http://127.0.0.1:8000/docs`
- Health check: `http://127.0.0.1:8000/health`

## 🔍 Проверка установки

### Проверка зависимостей

```bash
pip list | grep -E "(fastapi|sqlalchemy|pandas|sklearn)"
```

### Проверка базы данных

```bash
sqlite3 users.db ".tables"
```

Должны быть таблицы: `users`, `authors`, `author_interests`

### Проверка модели

```bash
curl http://localhost:8000/health
```

Должен вернуть:
```json
{
  "status": "healthy",
  "authors_count": <число>,
  "model_loaded": true
}
```

## 🐛 Решение проблем

### Ошибка: "ModuleNotFoundError: No module named 'fastapi'"

**Решение**: Убедитесь, что виртуальное окружение активировано и зависимости установлены:
```bash
source .venv/bin/activate
pip install -r requirements.txt
```

### Ошибка: "Model not loaded"

**Решение**: 
1. Проверьте, что папка `model/` существует и содержит все файлы
2. Проверьте логи при запуске приложения
3. Обучите модель заново: `python train_model.py`

### Ошибка: "Database is locked"

**Решение**: Закройте все соединения с БД (включая SQLite браузеры) и перезапустите приложение

### Ошибка при импорте CSV

**Решение**:
1. Убедитесь, что CSV файлы имеют правильную кодировку (UTF-8)
2. Проверьте, что файлы не повреждены
3. Убедитесь, что у вас достаточно места на диске

## 📦 Структура проекта

```
backend_academic/
├── app/                    # Основной код приложения
│   ├── __init__.py
│   ├── main.py            # FastAPI приложение
│   ├── models.py          # SQLAlchemy модели
│   ├── schemas.py         # Pydantic схемы
│   ├── auth.py           # Аутентификация
│   ├── database.py       # Настройка БД
│   └── recommender.py    # Сервис рекомендаций
├── model/                 # Модель рекомендаций (не в git)
├── .env                   # Переменные окружения (не в git)
├── users.db              # База данных (не в git)
├── *.csv                 # CSV файлы (не в git)
├── requirements.txt      # Зависимости Python
├── Dockerfile            # Docker конфигурация
├── README.md            # Основная документация
└── SETUP.md             # Этот файл
```

## 🔄 Обновление проекта

```bash
# Получить последние изменения
git pull

# Обновить зависимости
pip install --upgrade -r requirements.txt

# Перезапустить приложение
```

## 💡 Полезные команды

```bash
# Активировать виртуальное окружение
source .venv/bin/activate

# Деактивировать виртуальное окружение
deactivate

# Просмотр установленных пакетов
pip list

# Экспорт зависимостей (если добавили новые)
pip freeze > requirements.txt

# Запуск с отладкой
uvicorn app.main:app --reload --log-level debug
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи приложения
2. Проверьте документацию в README.md
3. Обратитесь к команде разработки

